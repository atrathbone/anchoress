export type IconLink = {
  iconUrl?: string, 
  linkUrl?: string, 
  text?: string
}

export type LiveDate = {
  date: Date, 
  venue: string, 
  location: string, 
  notes?: string,
  link?: string
}